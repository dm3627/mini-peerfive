<h1>Users</h1>
<table>
  <thead>
    <tr>
      <td>S.no</td>
      <td>Name</td>
      <td>P5 balance</td>
      <td>Reward balance</td>
      <td>Manage</td>
    </tr>
  </thead>
  <tbody>
    <%@users.each_with_index do |user, index|%>
      <tr>
        <td><%=index + 1%></td>
        <td><%= user.name%></td>
        <td><%= user.name%></td>
        <td><%= user.name%></td>
        <td><%= link_to "Edit", edit_user_path(user)%> <%= link_to 'Show', user%></td>
      </tr>
    <%end%>
  </tbody>
</table>
<br/>
<%= link_to "New User", new_user_path %>